%section#accomodation.widgit.white
  %h2= t(:accomodation)
  
  %p
    %span
      = "#{ t(:average_mortage) }: "

      = "$#{ @suburb["economy"]["averageMortage"] }"
      = t(:per_month)
      
    %br
    %span
      = "#{ t(:average_rental) }: "
      = "$#{ @suburb["economy"]["averageRental"] }"
      = t(:per_month)
    